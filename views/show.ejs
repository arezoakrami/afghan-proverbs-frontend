<!DOCTYPE html>
<html lang="fa">
<head>
  <meta charset="UTF-8">
  <title>جزئیات ضرب‌المثل</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light" dir="rtl">
  <div class="container mt-5">
    <h1 class="text-center mb-4"> جزئیات ضرب‌المثل</h1>

    <div class="card shadow-sm">
      <div class="card-body">
        <h3><%= proverb.textDari %></h3>
        <h5 class="text-muted"><%= proverb.textPashto %></h5>
        <p><strong>English:</strong> <%= proverb.translationEn %></p>
        <p><strong>معنی:</strong> <%= proverb.meaning %></p>
        <% let categories = proverb.category.split(','); %>
            <% categories.forEach(cat => { %>
            <span class="badge bg-info me-1"><%= cat.trim() %></span>
            <% }) %>

        <div class="mt-4">
          <a href="/proverbs/<%= proverb.id %>/edit" class="btn btn-warning"> ویرایش</a>

          <form action="/proverbs/<%= proverb.id %>?_method=DELETE" method="POST" class="d-inline">
            <button type="submit" class="btn btn-danger" onclick="return confirm('آیا مطمئنی؟')"> حذف</button>
          </form>

          <a href="/" class="btn btn-secondary">بازگشت</a>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
